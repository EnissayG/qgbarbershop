/**
 * Carte intégrée (Google Maps, sans clé API).
 * Coordonnées approx. du 7072 Sherbrooke E (H1N) pour éviter un mauvais géocodage.
 * Pour un repère parfait : Google Maps → Partager → Intégrer une carte → coller le `src` ici.
 */
export const GOOGLE_MAPS_EMBED_SRC =
  "https://www.google.com/maps?q=45.5871,-73.5403&hl=fr&z=17&output=embed";
